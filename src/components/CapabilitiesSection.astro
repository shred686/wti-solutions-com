---
export interface Props {
  backgroundStyle?: string;
  id?: string;
}

const { backgroundStyle, id } = Astro.props;
---

<section class="full-page-section h-screen flex items-center justify-center" data-section-type="capabilities" id={id} style={backgroundStyle || 'background: white;'}>
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
    <div class="text-center mb-8">
      <h2 class="text-4xl md:text-5xl font-light text-[#1a1a1a] mb-4">
        Connecting Innovation to Mission
      </h2>
      <p class="text-lg text-slate-charcoal-600 max-w-3xl mx-auto">
        WTI turns digital information assets into strategic insights through cutting-edge solutions that make data visible, accessible, understandable, interconnected, trusted, interoperable, and secure—driving innovation, confidence, and agility in a fast-paced, data-driven world.
      </p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 max-w-2xl mx-auto">
      <!-- Capability 1: Democratizing Data -->
      <div class="capability-card bg-slate-charcoal-50 p-4 rounded-lg border border-slate-charcoal-200 hover:shadow-lg transition-all duration-300 cursor-pointer group aspect-[4/3] relative overflow-hidden" data-capability="democratizing-data">
        <h3 class="text-lg font-medium text-slate-charcoal-900 text-center transition-all duration-300 transform absolute" style="top: 20px; left: 0; right: 0;">
          Democratizing Data
        </h3>
        <div id="democratizing-data-details" class="capability-details absolute inset-0 p-4 flex flex-col justify-start opacity-0 transition-all duration-300 transform translate-y-full">
          <div style="height: 20px;"></div>
          <p class="text-slate-charcoal-600 text-center leading-relaxed text-sm" style="margin-top: 20px;">
            WTI's democratized data solutions enable data to transition to machine-readable formats by building correlations between objects and activities of interest, allowingg decision-makers access to current intelligence.
          </p>
        </div>
      </div>

      <!-- Capability 2: AI Solutions -->
      <div class="capability-card bg-slate-charcoal-50 p-4 rounded-lg border border-slate-charcoal-200 hover:shadow-lg transition-all duration-300 cursor-pointer group aspect-[4/3] relative overflow-hidden" data-capability="ai-solutions">
        <h3 class="text-lg font-medium text-slate-charcoal-900 text-center transition-all duration-300 transform absolute" style="top: 20px; left: 0; right: 0;">
          AI Solutions
        </h3>
        <div id="ai-solutions-details" class="capability-details absolute inset-0 p-4 flex flex-col justify-start opacity-0 transition-all duration-300 transform translate-y-full">
          <div style="height: 20px;"></div>
          <p class="text-slate-charcoal-600 text-center leading-relaxed text-sm" style="margin-top: 20px;">
            WTI provides AI capabilities that suit the client's needs, including Natural Language Processing, Neural Networks, Machine Learning, Large Language Models and Expert Systems.
          </p>
        </div>
      </div>

      <!-- Capability 3: Enterprise Data Management -->
      <div class="capability-card bg-slate-charcoal-50 p-4 rounded-lg border border-slate-charcoal-200 hover:shadow-lg transition-all duration-300 cursor-pointer group aspect-[4/3] relative overflow-hidden" data-capability="enterprise-data">
        <h3 class="text-lg font-medium text-slate-charcoal-900 text-center transition-all duration-300 transform absolute" style="top: 20px; left: 0; right: 0;">
          Enterprise Data Management
        </h3>
        <div id="enterprise-data-details" class="capability-details absolute inset-0 p-4 flex flex-col justify-start opacity-0 transition-all duration-300 transform translate-y-full">
          <div style="height: 20px;"></div>
          <p class="text-slate-charcoal-600 text-center leading-relaxed text-sm" style="margin-top: 20px;">
            WTI has extensive expertise in transforming legacy enterprise architecture to architecture that can support automation, augmentation, and transformative AI solutions.
          </p>
        </div>
      </div>

      <!-- Capability 4: Serving the United States of Data -->
      <div class="capability-card bg-slate-charcoal-50 p-4 rounded-lg border border-slate-charcoal-200 hover:shadow-lg transition-all duration-300 cursor-pointer group aspect-[4/3] relative overflow-hidden" data-capability="united-states-data">
        <h3 class="text-lg font-medium text-slate-charcoal-900 text-center transition-all duration-300 transform absolute" style="top: 20px; left: 0; right: 0;">
          Serving the United States of Data
        </h3>
        <div id="united-states-data-details" class="capability-details absolute inset-0 p-4 flex flex-col justify-start opacity-0 transition-all duration-300 transform translate-y-full">
          <div style="height: 20px;"></div>
          <p class="text-slate-charcoal-600 text-center leading-relaxed text-sm" style="margin-top: 20px;">
            WTI makes disparate information understandable and available while ensuring security, privacy, and compliance with government guidelines. WTI is serving the United States of Data ®.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import { SectionAnimator } from '../utils/animations.js';
  
  // Initialize animation and event listeners when component loads
  document.addEventListener('DOMContentLoaded', () => {
    const section = document.querySelector('[data-section-type="capabilities"]');
    if (section) {
      const animator = new SectionAnimator();
      animator.initializeSection(section, 'capabilities');
    }
    
    // Add event listeners for capability cards
    const capabilityCards = document.querySelectorAll('.capability-card');
    capabilityCards.forEach(card => {
      const capabilityId = card.getAttribute('data-capability');
      const detailsElement = document.getElementById(`${capabilityId}-details`);
      const titleElement = card.querySelector('h3');
      
      card.addEventListener('mouseenter', () => {
        if (detailsElement) {
          detailsElement.classList.remove('opacity-0', 'translate-y-full');
          detailsElement.classList.add('opacity-100', 'translate-y-0');
        }
      });
      
      card.addEventListener('mouseleave', () => {
        if (detailsElement) {
          detailsElement.classList.remove('opacity-100', 'translate-y-0');
          detailsElement.classList.add('opacity-0', 'translate-y-full');
        }
      });
    });
  });
</script>

<style>
  .capability-card {
    transition: all 0.3s ease;
  }
  
  .capability-card:hover {
    transform: translateY(-2px);
  }
  
  .capability-details {
    transition: all 0.3s ease;
  }
</style> 
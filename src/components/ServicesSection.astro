---
import ServicesGrid from './ServicesGrid.astro';

export interface Props {
  backgroundImage?: string;
  overlayColor?: string;
  overlayOpacity?: number;
  backgroundStyle?: string;
  id?: string;
}

const { backgroundImage, overlayColor, overlayOpacity = 50, backgroundStyle, id } = Astro.props;

// Convert percentage to decimal for CSS
const overlayOpacityDecimal = overlayOpacity / 100;

// Set default overlay color only if overlayColor is provided
const finalOverlayColor = overlayColor || 'black';
---



<!-- 
HOW TO USE YOUR ICON PACK:

1. SVG Icons (Best for color control):
   - Place your SVG files in: src/assets/icons/
   - Import them like this:
   import DataGovernanceIcon from '../assets/icons/data-governance.svg?raw';
   - Use them in the component with currentColor for dynamic coloring

2. Icon Font (Easy to implement):
   - Add your icon font CSS to the head
   - Use classes like: <i class="icon-pack-data-governance"></i>

3. Image Icons (Limited color control):
   - Place PNG/SVG files in: public/assets/icons/
   - Use them as background images or <img> tags
-->

<section class="relative overflow-hidden bg-white" data-section-type="services" id={id}>
  <div class="w-full max-w-7xl mx-auto px-8 pt-12">
    <ServicesGrid />
  </div>
</section>

<script>
  // Component uses ScrollAnimator from IntroSection
</script>

 
